package org.genedb.querying.tmpquery;

import org.genedb.querying.core.HqlQuery;
import org.genedb.querying.core.QueryClass;
import org.genedb.querying.core.QueryParam;

@QueryClass(
        title="Coding and pseudogenes by protein mass",
        shortDesc="Get a list of transcripts ",
        longDesc=""
    )
public class ProteinMassQuery extends HqlQuery {

    @QueryParam(
            order=1,
            title="Minimum mass of protein"
    )
    private int min = 0;

    @QueryParam(
            order=2,
            title="Maximum mass of protein"
    )
    private int max = 1000;

    @Override
    protected String getHql() {
        return "select f.uniqueName from Feature f where f.cvTerm.name='polypeptide' and f.seqLen >= :min and f.seqLen <= :max";
    }

    // ------ Autogenerated code below here

    public void setMin(int min) {
        this.min = min;
    }

    public void setMax(int max) {
        this.max = max;
    }

	public int getMin() {
		return min;
	}

	public int getMax() {
		return max;
	}

	@Override
	protected String[] getParamNames() {
		return new String[] {"min", "max"};
	}

	@Override
	protected void populateQueryWithParams(org.hibernate.Query query) {
		query.setInteger("min", min);
		query.setInteger("max", max);
	}





}
